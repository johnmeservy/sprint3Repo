import homepage.models as hmod
from django.contrib import admin

print('<==========================================================================>')
print('<==========================================================================>')
# admin.register(hmod.User)
# admin.register(hmod.User)
